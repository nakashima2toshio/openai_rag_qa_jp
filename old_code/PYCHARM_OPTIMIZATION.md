# PyCharm パフォーマンス最適化ガイド

## 🔴 **深刻な問題を検出**（2025-10-04更新）

### 現在の症状
- **CPU使用率**: 450%以上（異常に高い）
- **ヒープメモリ使用率**: **98.6%** (12,122M / 12,288M) ⚠️
- **合計メモリ使用**: 13,710M（約**13.7GB**）
- **スワップ発生**: 約**4GB以上**
- **システムへの影響**: Mac全体の24GBメモリの**57%**を消費

### 根本原因
1. **ヒープメモリが限界に達している**（98.6%使用）
2. `.venv`ディレクトリ（1.2GB）がインデックス対象
3. `OUTPUT`（67MB）、`datasets`（23MB）もインデックス対象
4. ヒープ不足によるGCの頻発 → CPU使用率急増

---

## ✅ **緊急対処（必須）**

### 1. ヒープメモリの増量（最重要・即実施）

#### 手順:
1. **PyCharm** → **Help** → **Edit Custom VM Options...**
2. 以下の設定に変更:

```properties
# 現在の設定（推定）
-Xmx12288m   # ← これが問題！

# 推奨設定に変更
-Xmx16384m
-Xms2048m
-XX:ReservedCodeCacheSize=512m
-XX:+UseG1GC
-XX:SoftRefLRUPolicyMSPerMB=50
-XX:MaxMetaspaceSize=512m
-XX:ConcGCThreads=2
-XX:ParallelGCThreads=4
```

#### 設定の説明:
- `-Xmx16384m`: 最大ヒープメモリを**16GB**に増量
- `-Xms2048m`: 初期ヒープメモリを2GBに設定
- `-XX:ReservedCodeCacheSize=512m`: コードキャッシュを512MBに増量
- `-XX:+UseG1GC`: G1ガベージコレクタを使用
- `-XX:SoftRefLRUPolicyMSPerMB=50`: ソフトリファレンスのライフタイムを短縮
- `-XX:MaxMetaspaceSize=512m`: メタスペースの最大サイズを制限
- `-XX:ConcGCThreads=2`: 並行GCスレッド数を制限
- `-XX:ParallelGCThreads=4`: 並列GCスレッド数を制限

**⚠️ 重要**: 変更後は必ずPyCharmを再起動

### 2. PyCharm IDE設定の推奨事項

#### 即座に実施すべき設定:
1. **File > Settings > Appearance & Behavior > System Settings**
   - [ ] "Synchronize files on frame or editor tab activation" をOFF
   - [ ] "Save files on frame deactivation" をOFF
   - [ ] "Use safe write" をOFF

2. **File > Settings > Editor > General > Code Completion**
   - [ ] "Match case" をON（補完候補を減らす）
   - [ ] "Show suggestions as you type" をOFF（必要に応じて）

3. **File > Settings > Build, Execution, Deployment > Build Tools**
   - [ ] "Build project automatically" をOFF

4. **File > Settings > Project > Project Structure**
   - `.venv`フォルダを「Excluded」に設定
   - `__pycache__`フォルダを「Excluded」に設定
   - `old_code`フォルダを「Excluded」に設定
   - `.git`フォルダを「Excluded」に設定

### 3. インデックス最適化
```bash
# PyCharmのキャッシュをクリア（PyCharmを終了してから実行）
rm -rf ~/Library/Caches/JetBrains/PyCharm*
```

### 4. プラグインの最適化
**File > Settings > Plugins** で以下を確認:
- 使用していないプラグインを無効化
- 特に重いプラグイン（Database Tools、Docker等）を必要時のみ有効化

### 5. パワーセーブモード
一時的にCPU使用率を下げたい場合:
- **File > Power Save Mode** を有効化
- コード補完やリアルタイム検査が制限されるが、CPU使用率が大幅に低下

### 6. 追加の最適化オプション

#### プロセス優先度の調整（Terminal）:
```bash
# PyCharmのプロセスIDを確認
ps aux | grep -i pycharm | grep -v grep

# 優先度を下げる（PIDは実際の値に置き換え）
renice +10 -p [PID]
```

#### Activity Monitorでの監視:
1. Activity Monitorを開く
2. PyCharmプロセスを右クリック
3. "Sample Process"で詳細な負荷原因を確認

### 7. 代替案

#### 軽量エディタの併用:
- 大規模な編集作業: PyCharm
- 簡単な編集: VS Code、Sublime Text

#### PyCharm Community Editionの検討:
Professional版より軽量で、基本的なPython開発には十分

## 効果測定
設定変更後、以下を確認:
1. PyCharmを再起動
2. 10分程度通常作業を実施
3. Activity MonitorでCPU使用率を確認

## トラブルシューティング

### 症状が改善しない場合:
1. PyCharmを完全にアンインストール
2. 設定をリセット: `rm -rf ~/Library/Preferences/jetbrains.pycharm.*`
3. クリーンインストール

### 特定のファイルで重くなる場合:
- 大きなファイル（>2MB）を分割
- バイナリファイルを.gitignoreに追加
- ログファイルをExcludedフォルダに配置

## 定期メンテナンス
週1回実施:
- File > Invalidate Caches and Restart
- 不要なプロジェクトを閉じる
- タブ数を10個以下に維持